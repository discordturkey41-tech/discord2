<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= server.name %> - Setup Configuration</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        .page-content {
            width: 100%;
            min-height: calc(100vh - 60px);
        }

        .setup-full-page {
            width: 100%;
            padding: 40px 0;
            background: linear-gradient(135deg, rgba(10, 20, 40, 0.9), rgba(20, 35, 65, 0.9));
        }

        .setup-header {
            text-align: center;
            padding: 40px 20px;
            border-bottom: 2px solid rgba(0, 212, 255, 0.2);
            margin-bottom: 40px;
        }

        .setup-header h1 {
            color: #00d4ff;
            font-size: 2.5em;
            margin: 0 0 10px 0;
            background: linear-gradient(135deg, #00d4ff, #7c3aed);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .setup-header p {
            color: rgba(255, 255, 255, 0.6);
            margin: 0;
            font-size: 1.1em;
        }

        .setup-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
            gap: 30px;
            padding: 0 40px;
            max-width: 100%;
        }

        @media (max-width: 1200px) {
            .setup-grid {
                grid-template-columns: 1fr;
                padding: 0 30px;
            }
        }

        .config-card {
            background: linear-gradient(135deg, rgba(26, 42, 77, 0.6), rgba(39, 58, 99, 0.6));
            border: 1px solid rgba(0, 212, 255, 0.2);
            border-radius: 16px;
            padding: 30px;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .config-card:hover {
            border-color: rgba(0, 212, 255, 0.4);
            box-shadow: 0 10px 40px rgba(0, 212, 255, 0.1);
        }

        .config-card-title {
            font-size: 1.4em;
            color: #00d4ff;
            margin: 0 0 20px 0;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #00d4ff;
            font-weight: 600;
            font-size: 0.95em;
        }

        .form-group select {
            width: 100%;
            padding: 12px 15px;
            background: rgba(15, 23, 42, 0.8);
            border: 1px solid rgba(0, 212, 255, 0.3);
            border-radius: 8px;
            color: #ffffff;
            font-size: 1em;
            transition: all 0.3s ease;
            cursor: pointer;
            appearance: none;
            background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%2300d4ff' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
            background-repeat: no-repeat;
            background-position: right 10px center;
            background-size: 20px;
            padding-right: 40px;
        }

        .form-group select:focus {
            outline: none;
            border-color: #00d4ff;
            background-color: rgba(15, 23, 42, 1);
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.2);
        }

        .form-description {
            font-size: 0.85em;
            color: rgba(255, 255, 255, 0.5);
            margin-top: 5px;
            font-style: italic;
        }

        .button-group {
            display: flex;
            gap: 15px;
            margin-top: 50px;
            padding: 30px 40px;
            justify-content: center;
            border-top: 1px solid rgba(0, 212, 255, 0.1);
        }

        @media (max-width: 1200px) {
            .button-group {
                padding: 30px 20px;
            }
        }

        .btn {
            padding: 12px 40px;
            border: none;
            border-radius: 8px;
            font-size: 1em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-save {
            background: linear-gradient(135deg, #00d4ff, #0099ff);
            color: #000;
            box-shadow: 0 0 20px rgba(0, 212, 255, 0.4);
        }

        .btn-save:hover {
            box-shadow: 0 0 30px rgba(0, 212, 255, 0.6);
            transform: translateY(-2px);
        }

        .btn-back {
            background: rgba(255, 255, 255, 0.1);
            color: #ffffff;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .btn-back:hover {
            background: rgba(255, 255, 255, 0.15);
        }

        .alert {
            padding: 15px 20px;
            margin: 0 40px 25px 40px;
            border-radius: 8px;
            display: none;
            animation: slideIn 0.3s ease;
            position: fixed;
            top: 80px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1000;
            min-width: 300px;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-50%) translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateX(-50%) translateY(0);
            }
        }

        .alert.success {
            background: rgba(76, 175, 80, 0.9);
            border: 1px solid rgba(76, 175, 80, 0.5);
            color: #ffffff;
            display: block;
        }

        .alert.error {
            background: rgba(244, 67, 54, 0.9);
            border: 1px solid rgba(244, 67, 54, 0.5);
            color: #ffffff;
            display: block;
        }

        .nav-breadcrumb {
            display: flex;
            gap: 15px;
            padding: 20px 40px;
            flex-wrap: wrap;
            align-items: center;
            border-bottom: 1px solid rgba(0, 212, 255, 0.1);
        }

        .nav-link {
            padding: 8px 16px;
            background: rgba(26, 42, 77, 0.6);
            border: 1px solid rgba(0, 212, 255, 0.2);
            border-radius: 6px;
            color: #ffffff;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.95em;
        }

        .nav-link:hover,
        .nav-link.active {
            background: rgba(0, 212, 255, 0.2);
            border-color: #00d4ff;
            color: #00d4ff;
        }

        .select-list {
            max-height: 250px;
            overflow-y: auto;
        }

        .select-list option {
            padding: 10px;
            background: rgba(20, 30, 50, 1);
            color: #ffffff;
        }

        .select-list option:checked {
            background: rgba(0, 100, 150, 1);
            color: #ffffff;
        }
    </style>
</head>
<body>
    <!-- Loading Overlay -->
    <div class="loading-overlay" id="loadingOverlay">
        <div class="loading-content">
            <div class="loading-spinner"></div>
            <div class="loading-text">
                Loading<span class="loading-dots"><span>.</span><span>.</span><span>.</span></span>
            </div>
        </div>
    </div>

    <header>
        <nav>
            <div class="logo">⚡ Bot Command Center</div>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/servers">Servers</a>
                <a href="/logout">Logout</a>
            </div>
        </nav>
    </header>

    <div class="page-content hidden" id="pageContent">
        <!-- Navigation Breadcrumb -->
        <div class="nav-breadcrumb">
            <a href="/servers" class="nav-link">← Back to Servers</a>
            <a href="/server/<%= server.id %>" class="nav-link">📊 Server Dashboard</a>
            <a href="/server/<%= server.id %>/setup" class="nav-link active">⚙️ Setup Configuration</a>
        </div>

        <!-- Alerts -->
        <div class="alert success" id="successAlert">
            ✅ Settings saved successfully!
        </div>
        <div class="alert error" id="errorAlert">
            ❌ <span id="errorMessage"></span>
        </div>

        <div class="setup-full-page">
            <!-- Header -->
            <div class="setup-header">
                <h1>⚙️ Server Configuration</h1>
                <p>Configure your bot settings for <strong><%= server.name %></strong></p>
            </div>

            <!-- Setup Form -->
            <form class="setupForm">
                <div class="setup-grid">
                    <!-- Shop Configuration -->
                    <div class="config-card">
                        <h3 class="config-card-title">🛍️ Shop Configuration</h3>
                        
                        <div class="form-group">
                            <label>Shop Admin Role</label>
                            <select class="formInput" data-name="shopAdmin" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role allowed to manage shops</div>
                        </div>

                        <div class="form-group">
                            <label>Shop Mention Role</label>
                            <select class="formInput" data-name="shopMention" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role mentioned when new shops are created</div>
                        </div>

                        <div class="form-group">
                            <label>Shop Ticket Category</label>
                            <select class="formInput" data-name="shopTicket" class="select-list">
                                <option value="">Select a category...</option>
                            </select>
                            <div class="form-description">Category for shop purchase tickets</div>
                        </div>
                    </div>

                    <!-- Order Configuration -->
                    <div class="config-card">
                        <h3 class="config-card-title">📦 Order Configuration</h3>
                        
                        <div class="form-group">
                            <label>Order Admin Role</label>
                            <select class="formInput" data-name="orderAdmin" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role allowed to manage orders</div>
                        </div>

                        <div class="form-group">
                            <label>Order Mention Role</label>
                            <select class="formInput" data-name="orderMention" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role mentioned for order notifications</div>
                        </div>

                        <div class="form-group">
                            <label>Order Room Channel</label>
                            <select class="formInput" data-name="orderRoom" class="select-list">
                                <option value="">Select a channel...</option>
                            </select>
                            <div class="form-description">Channel where orders are managed</div>
                        </div>

                        <div class="form-group">
                            <label>Order Ticket Category</label>
                            <select class="formInput" data-name="orderTicket" class="select-list">
                                <option value="">Select a category...</option>
                            </select>
                            <div class="form-description">Category for order purchase tickets</div>
                        </div>
                    </div>

                    <!-- Auction Configuration -->
                    <div class="config-card">
                        <h3 class="config-card-title">🔨 Auction Configuration</h3>
                        
                        <div class="form-group">
                            <label>Auction Admin Role</label>
                            <select class="formInput" data-name="auctionAdmin" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role allowed to manage auctions</div>
                        </div>

                        <div class="form-group">
                            <label>Auction Mention Role</label>
                            <select class="formInput" data-name="auctionMention" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role mentioned for auction notifications</div>
                        </div>

                        <div class="form-group">
                            <label>Auction Ticket Category</label>
                            <select class="formInput" data-name="auctionTicket" class="select-list">
                                <option value="">Select a category...</option>
                            </select>
                            <div class="form-description">Category for auction purchase tickets</div>
                        </div>
                    </div>

                    <!-- Ads Configuration -->
                    <div class="config-card">
                        <h3 class="config-card-title">📢 Ads Configuration</h3>
                        
                        <div class="form-group">
                            <label>Ads Admin Role</label>
                            <select class="formInput" data-name="adAdmin" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role allowed to manage ads</div>
                        </div>

                        <div class="form-group">
                            <label>Ad Ticket Category</label>
                            <select class="formInput" data-name="adTicket" class="select-list">
                                <option value="">Select a category...</option>
                            </select>
                            <div class="form-description">Category for ad purchase tickets</div>
                        </div>
                    </div>

                    <!-- Roles & Permissions -->
                    <div class="config-card">
                        <h3 class="config-card-title">👑 Roles & Permissions</h3>
                        
                        <div class="form-group">
                            <label>Partner Role</label>
                            <select class="formInput" data-name="partnerRole" class="select-list">
                                <option value="">Select a role...</option>
                            </select>
                            <div class="form-description">Role for partner tier users</div>
                        </div>

                        <div class="form-group">
                            <label>Role Ticket Category</label>
                            <select class="formInput" data-name="roleTicket" class="select-list">
                                <option value="">Select a category...</option>
                            </select>
                            <div class="form-description">Category for role purchase tickets</div>
                        </div>
                    </div>

                    <!-- Logging & Warnings -->
                    <div class="config-card">
                        <h3 class="config-card-title">📝 Logging & Warnings</h3>
                        
                        <div class="form-group">
                            <label>Logs Channel</label>
                            <select class="formInput" data-name="logs" class="select-list">
                                <option value="">Select a channel...</option>
                            </select>
                            <div class="form-description">Channel where all bot actions are logged</div>
                        </div>

                        <div class="form-group">
                            <label>Maximum Warnings</label>
                            <input type="number" class="formInput" data-name="maximumWarnings" placeholder="e.g., 5" min="1" style="padding: 12px 15px; background: rgba(15, 23, 42, 0.8); border: 1px solid rgba(0, 212, 255, 0.3); border-radius: 8px; color: #ffffff; width: 100%;">
                            <div class="form-description">Maximum warnings before user action</div>
                        </div>
                    </div>

                    <!-- Tax Configuration -->
                    <div class="config-card">
                        <h3 class="config-card-title">💰 Tax Configuration</h3>
                        
                        <div class="form-group">
                            <label>Tax Percentage</label>
                            <input type="number" class="formInput" data-name="tax" placeholder="e.g., 10" min="0" max="100" style="padding: 12px 15px; background: rgba(15, 23, 42, 0.8); border: 1px solid rgba(0, 212, 255, 0.3); border-radius: 8px; color: #ffffff; width: 100%;">
                            <div class="form-description">Tax percentage on transactions</div>
                        </div>

                        <div class="form-group">
                            <label>Tax Time (milliseconds)</label>
                            <input type="number" class="formInput" data-name="taxTime" placeholder="e.g., 86400000" min="0" style="padding: 12px 15px; background: rgba(15, 23, 42, 0.8); border: 1px solid rgba(0, 212, 255, 0.3); border-radius: 8px; color: #ffffff; width: 100%;">
                            <div class="form-description">Frequency of tax collection (24h = 86400000ms)</div>
                        </div>
                    </div>

                    <!-- Bank & Transfer -->
                    <div class="config-card">
                        <h3 class="config-card-title">🏦 Bank & Transfer</h3>
                        
                        <div class="form-group">
                            <label>Bank Owner (User)</label>
                            <select class="formInput" data-name="bank" class="select-list">
                                <option value="">Select a user...</option>
                            </select>
                            <div class="form-description">User responsible for bank transfers</div>
                        </div>
                    </div>

                    <!-- Support Line -->
                    <div class="config-card">
                        <h3 class="config-card-title">📞 Support Line</h3>
                        
                        <div class="form-group">
                            <label>Support Line URL</label>
                            <input type="url" class="formInput" data-name="line" placeholder="https://..." style="padding: 12px 15px; background: rgba(15, 23, 42, 0.8); border: 1px solid rgba(0, 212, 255, 0.3); border-radius: 8px; color: #ffffff; width: 100%;">
                            <div class="form-description">URL or image for support line</div>
                        </div>
                    </div>
                </div>

                <!-- Sync Status Panel -->
                <div class="section mt-2xl">
                    <h2 class="section-title mb-xl">🔄 Data Sync Status</h2>
                    <div class="card" id="syncStatusPanel">
                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                            <div>
                                <div class="text-secondary" style="font-size: 12px;">Last Sync</div>
                                <div class="text-primary" style="font-size: 18px; font-weight: bold;" id="lastSyncTime">Loading...</div>
                            </div>
                            <div>
                                <div class="text-secondary" style="font-size: 12px;">Sync Status</div>
                                <div style="font-size: 18px; font-weight: bold;" id="syncStatusIcon">🔄 Syncing...</div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Buttons -->
                <div class="button-group">
                    <a href="/server/<%= server.id %>" class="btn btn-back">← Cancel</a>
                    <button type="button" id="refreshBtn" class="btn btn-secondary">🔄 Refresh Data</button>
                    <button type="button" id="clearBtn" class="btn btn-danger">🗑️ Clear Data</button>
                    <button type="submit" class="btn btn-save">💾 Save Configuration</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        const serverId = '<%= server.id %>';
        const setupForm = document.querySelector('.setupForm');
        const successAlert = document.getElementById('successAlert') || document.querySelector('.successAlert');
        const errorAlert = document.getElementById('errorAlert') || document.querySelector('.errorAlert');
        const errorMessage = document.getElementById('errorMessage') || document.querySelector('.errorMessage');
        const formInputs = document.querySelectorAll('.formInput');
        const selectInputs = Array.from(formInputs).filter(input => input.tagName === 'SELECT');

        let serverRoles = [];
        let serverChannels = [];
        let serverUsers = [];

        // Fetch server data (roles, channels, and users)
        async function fetchServerData() {
            try {
                const response = await fetch(`/api/server/${serverId}/roles-channels`);
                if (!response.ok) {
                    throw new Error(`API returned ${response.status}`);
                }
                const data = await response.json();
                
                serverRoles = data.roles || [];
                serverChannels = data.channels || [];
                serverUsers = data.users || [];
                
                populateSelects();
                // Load data AFTER selects are populated
                await loadSetupData();
            } catch (error) {
                console.error('Error fetching server data:', error);
                showError('Failed to load server roles and channels');
            }
        }

        // Populate all select dropdowns
        function populateSelects() {
            selectInputs.forEach(input => {
                const name = input.dataset.name;
                
                // Determine what type of items to use
                let items = [];
                let placeholder = 'Select an option...';
                
                if (name === 'bank') {
                    items = serverUsers;
                    placeholder = 'Select a user...';
                } else if (['shopTicket', 'orderTicket', 'auctionTicket', 'roleTicket', 'adTicket'].includes(name)) {
                    items = serverChannels.filter(c => c.type === 4); // Categories only
                    placeholder = 'Select a category...';
                } else if (['orderRoom', 'logs'].includes(name)) {
                    items = serverChannels.filter(c => c.type === 0); // Text channels only
                    placeholder = 'Select a channel...';
                } else {
                    // Roles
                    items = serverRoles;
                    placeholder = 'Select a role...';
                }
                
                // Clear existing options except the first one
                while (input.options.length > 1) {
                    input.remove(1);
                }
                
                // Add items
                items.forEach(item => {
                    const option = document.createElement('option');
                    option.value = item.id;
                    option.textContent = item.name; // No IDs in the name
                    input.appendChild(option);
                });
            });
        }

        // Load existing setup data
        async function loadSetupData() {
            try {
                const response = await fetch(`/api/server/${serverId}/setup`);
                if (!response.ok) {
                    console.debug('No setup data found');
                    return;
                }
                
                const data = await response.json();
                console.debug('Loaded setup data:', data);
                
                if (!data || !data.guildId) {
                    console.debug('Empty setup data');
                    return;
                }
                
                // List of all fields to check
                const fields = [
                    'shopAdmin', 'shopMention', 'shopTicket',
                    'orderAdmin', 'orderMention', 'orderRoom', 'orderTicket',
                    'auctionAdmin', 'auctionMention', 'auctionTicket',
                    'adAdmin', 'adTicket',
                    'partnerRole', 'roleTicket',
                    'logs', 'maximumWarnings',
                    'tax', 'taxTime',
                    'bank', 'line'
                ];
                
                // Set all field values
                fields.forEach(fieldName => {
                    const input = document.querySelector(`[data-name="${fieldName}"]`);
                    if (input) {
                        if (data[fieldName]) {
                            input.value = data[fieldName];
                            console.debug(`Set ${fieldName} to ${data[fieldName]}`);
                        } else {
                            // Clear the field if no data
                            input.value = '';
                            console.debug(`Cleared ${fieldName} (no data)`);
                        }
                    }
                });
                
            } catch (error) {
                console.error('Error loading setup data:', error);
            }
        }

        // Handle form submission
        setupForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            // Disable submit button during submission
            const submitBtn = setupForm.querySelector('button[type="submit"]');
            const originalText = submitBtn.textContent;
            submitBtn.disabled = true;
            submitBtn.textContent = '⏳ Synchronizing...';
            
            const data = {};
            formInputs.forEach(input => {
                const name = input.dataset.name;
                const value = input.value.trim();
                // Only include non-empty values, let the backend preserve existing data
                if (value) {
                    data[name] = value;
                }
            });
            
            try {
                const response = await fetch(`/api/server/${serverId}/setup`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(data)
                });
                
                if (!response.ok) {
                    const error = await response.json();
                    throw new Error(error.error || 'Failed to save settings');
                }
                
                const result = await response.json();
                showSuccess('✅ Setup synchronized successfully with bot database!');
                if (successAlert) {
                    setTimeout(() => {
                        successAlert.style.display = 'none';
                    }, 5000);
                }
            } catch (error) {
                console.error('Error saving settings:', error);
                showError(error.message || 'Failed to save settings');
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            }
        });

        function showSuccess(message) {
            if (successAlert) {
                if (message) {
                    successAlert.textContent = message;
                }
                successAlert.style.display = 'block';
                if (errorAlert) errorAlert.style.display = 'none';
            }
        }

        function showError(message) {
            if (errorMessage) errorMessage.textContent = message;
            if (errorAlert) {
                errorAlert.style.display = 'block';
                if (successAlert) successAlert.style.display = 'none';
            }
        }

        // Clear notifications
        function clearNotifications() {
            if (successAlert) successAlert.style.display = 'none';
            if (errorAlert) errorAlert.style.display = 'none';
        }

        function hideLoadingOverlay() {
            const loadingOverlay = document.getElementById('loadingOverlay');
            const pageContent = document.getElementById('pageContent');
            if (loadingOverlay) loadingOverlay.classList.add('hidden');
            if (pageContent) pageContent.classList.remove('hidden');
        }

        // Load data on page load
        window.addEventListener('load', async () => {
            try {
                await fetchServerData();
            } catch (error) {
                console.error('Error loading setup data:', error);
            }
            // Hide loading overlay after data is loaded
            hideLoadingOverlay();
        });

        // Sync monitoring
        async function updateSyncStatus() {
            try {
                const response = await fetch(`/api/server/${serverId}/last-sync`);
                if (response.ok) {
                    const data = await response.json();
                    const statusIcon = document.getElementById('syncStatusIcon');
                    const lastSyncTime = document.getElementById('lastSyncTime');
                    
                    if (statusIcon) {
                        statusIcon.textContent = '✅ Synced';
                        statusIcon.style.color = '#00ff00';
                    }
                    if (lastSyncTime) {
                        lastSyncTime.textContent = data.lastSyncAgo || 'Just now';
                    }
                }
            } catch (error) {
                console.debug('Sync status check error:', error);
            }
        }

        // Update sync status every 10 seconds
        setInterval(updateSyncStatus, 10000);
        updateSyncStatus();

        // Refresh button functionality
        const refreshBtn = document.getElementById('refreshBtn');
        if (refreshBtn) {
            refreshBtn.addEventListener('click', async () => {
                const originalText = refreshBtn.textContent;
                refreshBtn.disabled = true;
                refreshBtn.textContent = '⏳ Refreshing...';
                
                try {
                    await fetchServerData();
                    showSuccess('✅ Data refreshed successfully!');
                    updateSyncStatus();
                    setTimeout(() => {
                        if (successAlert) successAlert.style.display = 'none';
                    }, 3000);
                } catch (error) {
                    console.error('Error refreshing data:', error);
                    showError('Failed to refresh data');
                } finally {
                    refreshBtn.disabled = false;
                    refreshBtn.textContent = originalText;
                }
            });
        }

        // Clear button functionality
        const clearBtn = document.getElementById('clearBtn');
        if (clearBtn) {
            clearBtn.addEventListener('click', async () => {
                if (!confirm('⚠️ Are you sure? This will delete ALL setup data for this server.')) {
                    return;
                }

                const originalText = clearBtn.textContent;
                clearBtn.disabled = true;
                clearBtn.textContent = '⏳ Clearing...';
                
                try {
                    const response = await fetch(`/api/server/${serverId}/setup`, {
                        method: 'DELETE',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });
                    
                    if (!response.ok) {
                        throw new Error('Failed to clear data');
                    }
                    
                    // Clear all form fields
                    formInputs.forEach(input => {
                        input.value = '';
                    });
                    
                    showSuccess('✅ All setup data cleared successfully!');
                    setTimeout(() => {
                        if (successAlert) successAlert.style.display = 'none';
                    }, 3000);
                } catch (error) {
                    console.error('Error clearing data:', error);
                    showError('Failed to clear data');
                } finally {
                    clearBtn.disabled = false;
                    clearBtn.textContent = originalText;
                }
            });
        }
    </script>
</body>
</html>
